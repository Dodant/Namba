# Both the CI suite and the Dockerfile install this file, so an unpinned line
# means the image that ships is not the one the tests passed on. Transitives are
# pinned too: `fastapi` asks for `starlette>=0.46.0` with no ceiling.
#
# Moving a version means regenerating the whole file, so the closure stays a
# resolved set rather than a hand-edited one, then running test_namba.py on it:
#
#   uv venv --python 3.11 /tmp/pin && VIRTUAL_ENV=/tmp/pin uv pip install \
#     'fastapi==X' 'uvicorn[standard]==X' 'python-multipart==X' 'httpx==X' \
#     && VIRTUAL_ENV=/tmp/pin uv pip freeze | sort

# The four that are actually asked for. Nothing imports httpx: it is what
# starlette's TestClient runs on, so it is here for the suite and rides into the
# image unused. Starlette 1.6 wants httpx2 there now -- a move to make on
# purpose rather than by drift, which is what the pin is for.
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.32
httpx==0.28.1

# Everything the four pull in.
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
idna==3.19
pydantic==2.13.5
pydantic-core==2.46.5
python-dotenv==1.2.3
pyyaml==6.0.3
starlette==1.6.0
typing-extensions==4.16.0
typing-inspection==0.4.4
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1
